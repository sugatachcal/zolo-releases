# Zolo — Releases

Compiled desktop release artifacts and the auto-update feed (`latest.yml` / `dev.yml`) for the [Zolo](https://github.com/sugatachcal/zolo) desktop app.

**No source lives here** — only build outputs published by CI. See the [Releases](https://github.com/sugatachcal/zolo-releases/releases) tab.
